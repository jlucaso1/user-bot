version: 1

AppDir:
  path: ./AppDir

  app_info:
    id: xstro
    name: xstro
    icon: xstro
    version: "${VERSION}"
    exec: usr/bin/xstro
    exec_args: $@

  files:
    include:
      - usr/bin/xstro
      - usr/share/icons/xstro.png

AppImage:
  file_name: xstro-${VERSION}.AppImage
